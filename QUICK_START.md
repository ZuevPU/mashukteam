# Быстрый старт

## Шаг 1: Установка зависимостей

```bash
# Установить зависимости для backend и frontend
npm run install:all
```

Или вручную:
```bash
cd backend && npm install
cd ../frontend && npm install
```

## Шаг 2: Настройка Supabase

### 2.1. Создание аккаунта и проекта

1. **Откройте [supabase.com](https://supabase.com)** в браузере
2. **Нажмите кнопку "Start your project"** или "Sign in" (если уже есть аккаунт)
3. **Войдите через GitHub** (рекомендуется) или создайте аккаунт через email
4. **После входа нажмите "New Project"** (зеленая кнопка в правом верхнем углу)
5. **Заполните форму создания проекта:**
   - **Name**: введите название проекта (например: `telegram-miniapp`)
   - **Database Password**: придумайте надежный пароль (⚠️ **ОБЯЗАТЕЛЬНО СОХРАНИТЕ ЕГО!**)
   - **Region**: выберите ближайший регион (например: `West US` или `West Europe`)
   - **Pricing Plan**: выберите `Free` (для начала достаточно)
6. **Нажмите "Create new project"** и подождите 1-2 минуты, пока проект создается

### 2.2. Выполнение миграции (создание таблицы users)

1. **В левом боковом меню найдите раздел "SQL Editor"** (иконка с символом `</>` или текст "SQL Editor")
2. **Нажмите на "SQL Editor"** - откроется редактор SQL
3. **Нажмите кнопку "New query"** (если она есть) или просто кликните в область редактора
4. **Откройте файл** `supabase/migrations/001_create_users_table.sql` в вашем проекте (в редакторе кода)
5. **Скопируйте ВЕСЬ содержимое файла** (Ctrl+A, затем Ctrl+C)
6. **Вставьте SQL код в редактор Supabase** (Ctrl+V)
7. **Нажмите кнопку "Run"** (или `Ctrl+Enter`) в правом нижнем углу редактора
8. **Проверьте результат:**
   - Должно появиться сообщение "Success. No rows returned" или похожее
   - Если есть ошибки - проверьте, что скопировали весь файл целиком

### 2.3. Получение Project URL

1. **В левом боковом меню найдите раздел "Settings"** (иконка шестеренки ⚙️)
2. **Нажмите на "Settings"**, затем выберите **"API"** в подменю
3. **Найдите раздел "Project URL"** (обычно в самом верху страницы)
4. **Скопируйте URL** - он выглядит примерно так: `https://abcdefghijklmnop.supabase.co`
5. **Сохраните этот URL** - он понадобится для `SUPABASE_URL` в `.env` файле

### 2.4. Получение Service Role Key

1. **На той же странице "Settings → API"** прокрутите вниз
2. **Найдите раздел "Project API keys"**
3. **Найдите ключ с названием "service_role"** (⚠️ **ВНИМАНИЕ: это секретный ключ!**)
4. **Нажмите на иконку глаза 👁️** рядом с ключом, чтобы показать его (или кнопку "Reveal")
5. **Скопируйте весь ключ** - он выглядит как длинная строка: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
6. **Сохраните этот ключ** - он понадобится для `SUPABASE_SERVICE_KEY` в `.env` файле
   - ⚠️ **ВАЖНО**: Никогда не публикуйте этот ключ в открытом доступе (GitHub, публичные репозитории)!

### 2.5. Проверка созданной таблицы

1. **В левом боковом меню найдите раздел "Table Editor"** (иконка таблицы 📊)
2. **Нажмите на "Table Editor"**
3. **Вы должны увидеть таблицу `users`** в списке таблиц
4. **Нажмите на таблицу `users`** - откроется структура таблицы
5. **Проверьте, что в таблице есть следующие колонки:**
   - `id` (uuid)
   - `telegram_id` (int8)
   - `telegram_username` (text)
   - `first_name` (text)
   - `last_name` (text)
   - `middle_name` (text)
   - `motivation` (text)
   - `status` (text)
   - `created_at` (timestamptz)
   - `updated_at` (timestamptz)

✅ **Если таблица создана и все колонки на месте - настройка Supabase завершена!**

### Что дальше?

Теперь у вас есть:
- ✅ **SUPABASE_URL** - URL вашего проекта
- ✅ **SUPABASE_SERVICE_KEY** - секретный ключ для доступа

Эти значения нужно будет вставить в файл `backend/.env` на следующем шаге (см. Шаг 4).

## Шаг 3: Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте бота: `/newbot`
3. Получите Bot Token → `TELEGRAM_BOT_TOKEN`
4. Создайте Mini App: `/newapp`
   - Выберите вашего бота
   - Укажите название
   - URL укажите позже (после деплоя frontend)

## Шаг 4: Настройка Backend

```bash
cd backend
cp .env.example .env
```

Отредактируйте `backend/.env`:
```env
PORT=3000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key
TELEGRAM_BOT_TOKEN=8579617830:AAGtJGXl03X1KktX994NEfbJVz7GZL2qZN8
CORS_ORIGIN=http://localhost:5173
```

Запуск:
```bash
npm run dev
```

Backend будет доступен на `http://localhost:3000`

## Шаг 5: Настройка Frontend

```bash
cd frontend
cp .env.example .env
```

Отредактируйте `frontend/.env`:
```env
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=https://your-project.supabase.co
```

Запуск:
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## Шаг 6: Тестирование

### Вариант 1: Через ngrok (для тестирования Mini App)

1. Установите [ngrok](https://ngrok.com)
2. Запустите туннель:
   ```bash
   ngrok http 5173
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. В BotFather обновите URL Mini App на этот адрес
5. Откройте бота в Telegram и нажмите кнопку Mini App

### Вариант 2: Локальное тестирование (без Mini App)

1. Откройте `http://localhost:5173` в браузере
2. Приложение будет работать, но без Telegram WebApp SDK
3. Для полного тестирования используйте ngrok

## Проверка работы

1. ✅ Backend отвечает на `http://localhost:3000/health`
2. ✅ Frontend открывается в браузере
3. ✅ В консоли браузера нет критических ошибок
4. ✅ В консоли backend видны логи запросов

## Следующие шаги

- Прочитайте `ARCHITECTURE.md` для понимания архитектуры
- Изучите `SETUP.md` для детальной настройки
- Начните разработку геймификации (см. комментарии в коде)

## Проблемы?

### Backend не запускается
- Проверьте, что все переменные в `.env` заполнены
- Убедитесь, что порт 3000 свободен
- Проверьте подключение к Supabase

### Frontend не подключается к API
- Убедитесь, что backend запущен
- Проверьте `VITE_API_URL` в `.env`
- Проверьте CORS настройки в backend

### Ошибки в Supabase
- Убедитесь, что миграция выполнена
- Проверьте права доступа Service Role Key
- Проверьте структуру таблицы в Supabase Dashboard
